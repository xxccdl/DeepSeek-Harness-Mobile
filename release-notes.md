# DeepSeek Harness Mobile v1.0.4

## 悬浮球优化

### 修复
- **初始化失败 (exit 2)**：仓库无 `.gitattributes` 时 Windows `autocrlf` 把 `init-termux.sh` 存成 CRLF 换行，升级重装后重跑初始化脚本直接语法报错。已将脚本归一化为 LF、新增 `.gitattributes`（`*.sh eol=lf`），并在原生复制脚本时运行时兜底去除 `\r`（三重防护）
- **标签误显示「null」**：AI 结束工具调用开始输出文字时，悬浮球工具标签偶尔显示 "null"——根因是 Android `org.json` 的 `optString()` 对 JSON null 返回字面量字符串而非回退值，已在原生侧归一化（空串 / "null" / "undefined" 一律视为无工具）
- **长按菜单无法关闭**：新增全屏透明遮罩层，点击菜单外任意空白处即可关闭（此前只能点标题收起）
- **菜单状态过期**：菜单打开期间 dsh 服务 / 当前工具状态行随轮询实时刷新
- **旋转 / 分屏错位**：屏幕尺寸变化后自动钳制位置并重新吸附到原贴边侧
- **拖出屏幕**：拖动过程增加边界钳制，球不再被拖出可视区域

### 性能
- **工具标签事件驱动**：「AI 正在使用的工具」从 2.5 秒轮询改为 `FileObserver` 监视状态文件（原子写入），显示延迟从最高 2.5s 降为实时
- **探活指数退避**：dsh 服务探活失败时轮询间隔 ×2 递增（2.5s → 上限 10s），引擎关闭时不再空转高频打点

### 体验
- **位置记忆**：吸附落边后持久化位置，重启 App 后悬浮球回到原位
- **触感反馈**：长按弹出面板、吸附落边时有轻微震动确认

## 安装
下载 `app-release.apk` 直接安装覆盖即可（Gitee 为分卷压缩，全部下载后解压）。
