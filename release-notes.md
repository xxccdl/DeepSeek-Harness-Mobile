# DeepSeek Harness Mobile v1.0.5

## 模型类型自动识别（多模态）

### 新增
- **自动识别模型类型**：在「设置 → 模型」里对自定义提供方点「获取可用模型」时，自动解析接口返回的 `input_modalities`，多模态（视觉）模型会被正确识别并标注「图像输入」能力，不再一律当作纯文本模型
- **模型「图像输入」开关**：每个模型行的展开区新增「图像输入」开关，可手动标记/修正模型是否支持图片输入（用于视觉模型或网关能力与官方标注不一致时）

### 修复
- 多模态模型被误判为不支持图像：`read_image` / `phone_screenshot` 依赖模型声明的图像输入能力，此前自定义提供方的多模态模型因能力标注缺失而被拒绝读取图片，现已全链路透传

## 手机控制：视觉控制模式

- **视觉控制**：使用支持视觉的多模态模型时，AI 可调用 `phone_screenshot` 截取手机屏幕并直接查看画面，识别图标、图片、图表与整体布局，再配合点击/滑动/输入完成操控；不再局限于无障碍树里的文字元素
- 「手机控制」设置页新增「截取屏幕画面」操作入口与视觉模式说明

## 欢迎向导不再重复弹出

- 首次使用向导的完成状态改为持久化保存（此前受端口变化影响每次都重新弹出），老用户已配置模型密钥的也会自动跳过

## 安装
下载 `app-release.apk` 直接安装覆盖即可（Gitee 为分卷压缩，全部下载后解压）。
