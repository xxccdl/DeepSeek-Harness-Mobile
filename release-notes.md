# DeepSeek Harness Mobile v1.0.7

## 新增

- **模型上下文窗口自动获取 / 手动填写**：模型设置里新增「自动获取上下文窗口」按钮，从模型目录或提供方 `/models` 接口自动拉取并填入每个模型的上下文窗口；获取不到的模型可在行内（容量区域）手动填写
- **血条支持自定义模型**：余额条现在按当前会话使用的模型提供商动态查询余额；无公开余额接口的提供商（如自定义网关）血条降级为仅显示本地已用估算，不再消失

## 修复

- **修复手机截图报错**：修复 `phone_screenshot` 报「Unsupported or malformed image data」的问题——Android 端无 sharp 原生库时改用纯 JS 头部解析校验图片，截图可正常直接发给多模态模型查看
- **悬浮球工具调用显示更友好**：工具结束后，悬浮球上的「AI 正在使用的工具」标签停留 1 秒再淡出消失，方便看清刚用的工具；期间有新的工具调用会立即切换

## 安装
下载 `app-release.apk` 直接安装覆盖即可（Gitee 为分卷压缩，全部下载后解压）。
